# 개발 시에는 비워 둔다. Vite 프록시가 /api, /images 를 localhost:8090 으로 넘겨주므로
# 브라우저 입장에서 동일 오리진이 되어 CORS/쿠키 문제가 발생하지 않는다.
VITE_API_BASE=

# 백엔드에 CORS 가 적용된 뒤(BE-01 해결) 직접 호출로 전환하려면:
# VITE_API_BASE=http://localhost:8090
